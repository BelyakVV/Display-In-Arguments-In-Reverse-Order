import java.util.Scanner;/*cистема консольного ввода реализуется с использованием класса Scanner.
Класс должен быть испортирован в прогрумму, для чего используется вышеуказанная инструкция.
*/

public class DisplayingrgumentsInReverseOrder { //мой класс "Отображение аргументов в обратном порядке"

    public static void main(String[] args) {
        System.out.print("Enter argument and press <Enter>:");//введите аргумент и нажмите ENTER.
        Scanner inputString = new Scanner(System.in);/*Его функциональность очень проста. 
        Он, словно настоящий сканер, считывает данные из источника, который ты для него укажешь. 
        Например, из строки, из файла, из консоли. Далее он распознает эту информацию и обрабатывает нужным образом.
        */
        String name = inputString.nextLine();/*наш метод обращается к источнику данных 
        (в нашем случае это командная строка), находит там следующую строку, которую он еще не считывал 
        (в нашем случае — первую) и возвращает ее. После чего выводит ее на консоль.
        */
        {/*примечание: если не выделить фрагмент кода ниже в фигурные скобки {}, то результов будет несколько,
            пока цикл не выполнит условие.
            */ 
        var index = name.length() - 1;//записываем в новую переменную индекс последнего символа строки (напомним, что индексы начинаются с нуля).
        var reversedString = "";//инициализируем строку, куда будем записывать результат.
        while (index >= 0) {//условие — повторяем тело цикла, пока текущий индекс не дошёл до 0, то есть до первого символа.
            var currentChar = name.charAt(index);/*берём из строки символ по индексу, в качестве индекса выступает текущее значение 
            счетчика - то есть в каждой итерации индекс будет разный.
            */
            reversedString = reversedString + currentChar;//добавляем в строку-результат этот символ.
            index = index - 1;//обновляем счетчик.
        }
    System.out.println (reversedString);//когда цикл завершился, выводим результат на командную строку.
    inputString.close();//сканер должен быть закрыт по завершении своей работы.
    }   
    }
}
